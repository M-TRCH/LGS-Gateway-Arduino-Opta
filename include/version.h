#ifndef VERSION_H
#define VERSION_H

// Reported by `$LGS PING` and `$LGS INFO`, and shown in the Test Tool's
// Gateway tab. Bump on a real release, not on every commit.
#define GW_FW_VERSION   "1.12.0"
#define GW_FW_BUILD     __DATE__

#endif // VERSION_H
